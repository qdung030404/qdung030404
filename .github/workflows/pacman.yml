name: Generate Pacman contribution graph

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: Platane/snk@v3
        with:
          github_user_name: qdung030404
          outputs: |
            dist/breakout-contribution-graph.svg?palette=github&outputType=puzzle-bobble
            dist/breakout-contribution-graph-dark.svg?palette=github-dark&outputType=puzzle-bobble

      - uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: pacman-output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
